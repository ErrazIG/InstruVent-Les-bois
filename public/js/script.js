const categGrid = document.querySelectorAll(".caetg-liens")
const titleCategImg = document.querySelectorAll(".title-img-categ")

categGrid.addEventListener("mouseover", (event) => {
    titleCategImg.style.color= "#fac21b"
});

onmouseover = (event) => {
    titleCategImg.style.color= "black"
};